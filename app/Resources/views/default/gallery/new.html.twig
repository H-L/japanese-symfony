{% extends 'default/base.html.twig' %}

{% form_theme form 'form/image_form.html.twig' %}

{% block body %}
    <h1>Gallery creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('galleries_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
